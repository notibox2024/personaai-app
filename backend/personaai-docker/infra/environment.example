# PostgreSQL Configuration
POSTGRES_DB=personaai
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_MULTIPLE_DATABASES=personaai,keycloak

# Keycloak Configuration
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin123
KC_HOSTNAME=localhost
KC_HOSTNAME_PORT=8080

# PostgREST Configuration
PGRST_DB_URI=postgres://postgres:postgres@postgres:5432/personaai
PGRST_DB_SCHEMAS=public,api
PGRST_DB_ANON_ROLE=web_anon
PGRST_JWT_SECRET=your-jwt-secret-key-change-this-in-production
PGRST_JWT_AUD=postgrest

# Service Ports
POSTGRES_PORT=5432
REDIS_PORT=6379
KEYCLOAK_PORT=8080
POSTGREST_PORT=3300

# Production Settings (uncomment for production)
# KC_HOSTNAME_STRICT=true
# KC_HOSTNAME_STRICT_HTTPS=true
# PGRST_OPENAPI_SECURITY_ACTIVE=true 